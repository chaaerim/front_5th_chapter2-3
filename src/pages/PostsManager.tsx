import { PostDashboard } from "@widgets/post"
import { Layout } from "@widgets/ui"

const PostsManager = () => {
  return (
    <Layout>
      <PostDashboard />
    </Layout>
  )
}

export default PostsManager
